; Allow the browser UI to persist tree state directly in CouchDB.
[chttpd]
enable_cors = true

[cors]
origins = *
credentials = false
methods = GET, PUT, POST, HEAD, DELETE, OPTIONS
headers = accept, authorization, content-type, origin, referer, x-csrf-token, x-requested-with
max_age = 3600
